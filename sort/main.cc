#include "sort.h"
#include <vector>
using namespace std;

// 归并排序
void merge_sort(vector<int> &arr) {

}

int main() {
    // test1
    vector<int> test1 = {9, 8, 7, 6, 5, 4, 3, 2, 1};
    merge_sort(test1);
    show_arr(test1);
}